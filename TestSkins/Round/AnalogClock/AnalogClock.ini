; Original Deskset test skin: analog clock showing the current time.
; Hour and minute hands are Roundline meters, the second hand is a Rotator (see Clock.inc).
[Variables]
; Empty = now. Fixed.ini sets a time (HH:MM:SS) for reproducible renders.
FixedTime=
@Include=#CURRENTPATH#Clock.inc
